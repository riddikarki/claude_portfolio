# Secrets — never commit these
.env
token.json
credentials.json

# Python
__pycache__/
*.pyc
.venv/

# OS noise
.DS_Store
Thumbs.db

# Runtime state on the VM (proposals, approved agents, board minutes)
state/
